# Minimal dependencies to run the Streamlit demo app.
# For full project development (training, evaluation, notebooks), use the
# requirements.txt at the project root instead.

streamlit>=1.28.0
torch>=2.1.0
torchvision>=0.16.0
pillow>=10.0.0
numpy>=1.24.0
matplotlib>=3.7.0
plotly>=5.22.1